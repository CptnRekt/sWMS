<Window x:Class="sWMS.Windows.AllDocumentsList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:sWMS.Windows"
        mc:Ignorable="d"
        Title="NazwaMagazynu" Height="450" Width="800">
    <Grid HorizontalAlignment="Stretch" Margin="0,0,0,0" Width="Auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="40" MaxHeight="80"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0 10 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="25"></RowDefinition>
            </Grid.RowDefinitions>
            <Button x:Name="addStudentButton" Grid.Column="0" Content="Utwórz nowy dokument" HorizontalAlignment="Center" VerticalAlignment="Bottom" Click="addStudentButton_Click"/>
            <Button x:Name="addGrade" Grid.Column="1" Content="Zapisz zmiany" HorizontalAlignment="Center" VerticalAlignment="Bottom" Click="addGrade_Click"/>
            <Button x:Name="removeStudentButton" Grid.Column="2" Content="Usuń zaznaczone pozycje" HorizontalAlignment="Center" VerticalAlignment="Bottom" Click="removeStudent"/>
            <TextBox x:Name="findStudentTextBox" Grid.Column="3" TextWrapping="Wrap" Text="" Width="120" Background="#FFC8C8C8" TextAlignment="Center" BorderThickness="1,1,1,1" VerticalAlignment="Bottom" MaxLength="100" MaxLines="1"/>
            <Button x:Name="findStudentButton" Grid.Column="4" Content="Wyszukaj dokument" HorizontalAlignment="Center" VerticalAlignment="Bottom" Click="findStudentButton_Click"/>
        </Grid>
        <DataGrid x:Name="StudentsDataGrid" Grid.Row="2" ItemsSource="{Binding Students}" CellEditEnding="StudentsDataGrid_CellEditEnding" AutoGenerateColumns="False" CanUserAddRows="False" BeginningEdit="StudentsDataGrid_BeginningEdit">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Dodaj studenta" Click="addStudentButton_Click" />
                    <MenuItem Header="Usuń studenta" Click="removeStudent" />
                    <MenuItem Header="Dodaj oceny" Click="addGrade_Click" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Path=IsSelected, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn x:Name="NameTextColumn" Binding="{Binding Name}" Header="Numer dokumentu" IsReadOnly="True"></DataGridTextColumn>
                <!--jedna kol na magazyn przy wybraniu magazynu (źródłowy / docelowy - w zależności czy wybrano przyjecia czy wydania)-->
                <DataGridTextColumn x:Name="SurnameTextColumn" Binding="{Binding Surname}" Header="Adresat"></DataGridTextColumn>
                <DataGridTextColumn x:Name="b" Binding="{Binding Surname}" Header="Odbiorca"></DataGridTextColumn>
                <DataGridTextColumn x:Name="GradesTextColumn" Binding="{Binding GradesString}" Header="Data utworzenia"></DataGridTextColumn>
                <DataGridTextColumn x:Name="DateOfBirthTextColumn" Binding="{Binding DateOfBirth}" Header="Data wydania"></DataGridTextColumn>
                <DataGridTextColumn x:Name="a" Binding="{Binding a}" Header="Postęp realizacji"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock TextWrapping="Wrap" Grid.Row="4" Text="Znaleziono 5 dopasowań   |   Usunięto 2 dokumenty   |   Masz 5 niezapisanych komórek." Margin="5 5 5 0"/>
    </Grid>
</Window>
